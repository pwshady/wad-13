package models

data class WADVersionFileData(
    var typeFile: String = "",
    var codeFile: Int = 404,
    var lengthFile: Long = 0,
    var dateFile: Long = 19970000000000,
    var statusFile: Int = 0,
)